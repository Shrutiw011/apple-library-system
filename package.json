{
  "name": "library-management-system",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  },
  "dependencies": {
    "dexie": "^4.4.2",
    "gh-pages": "^6.3.0",
    "lucide": "^1.8.0"
  }
}
